import 'package:flutter/material.dart';

class PaginaPrincipalMitjana extends StatelessWidget {
  const PaginaPrincipalMitjana({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),

      body: Center(child: Text("Pantalla Mitjana")),
    );
  }
}