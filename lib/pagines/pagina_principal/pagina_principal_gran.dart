import 'package:flutter/material.dart';

class PaginaPrincipalGran extends StatelessWidget {
  const PaginaPrincipalGran({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Mida gran"),),
    );
  }
}